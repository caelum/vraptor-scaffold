package app.models;

import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;

@Entity
public class <%= @model %> {

	@Id @GeneratedValue
	private Long id;
<% @attributes.each_key do |key| %>
	private <%= @attributes[key] %> <%= key %>;
<% end %>
	public void setId(Long id) {
		this.id = id;
	}

	public Long getId() {
		return id;
	}
<% @attributes.each_key do |key| %>
	public void set<%= key.to_s.capitalize %> (<%= @attributes[key] %> <%= key %>) {
		this.<%= key %> = <%= key %>;
	}
	
	public <%= @attributes[key] %> get<%= key.to_s.capitalize %>() {
		return <%= key %>;
	}
<% end %>
}
